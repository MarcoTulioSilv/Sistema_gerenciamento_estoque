# ============================================================
# SCE — Sistema de Controle de Estoque
# requirements.txt — dependências Python
# Instalar: pip install -r requirements.txt
# ============================================================
 
# Interface gráfica desktop
customtkinter==5.2.2
 
# Banco de dados
SQLAlchemy==2.0.36
PyMySQL==1.1.1          # driver MySQL para SQLAlchemy
 
# Segurança
bcrypt==4.2.1
cryptography==43.0.3    # Fernet — criptografia do App Password Gmail
 
# Scheduler de tarefas (notificações e relatórios agendados)
APScheduler==3.10.4
 
# Geração de planilhas XLSX
openpyxl==3.1.5
 
# Parser de XML NF-e
lxml==5.3.0
 
# Utilitários
python-dotenv==1.0.1    # leitura de variáveis de ambiente

# MOD-07 · Patrimônio — etiquetas (QR Code + Code 128)
segno==1.6.1            # geração de QR Code
python-barcode==0.15.1  # geração de Code 128
reportlab==4.2.5        # PDF vetorial (etiqueta unitária e folha A4)
pywin32==308            # spooler do Windows em modo RAW (impressão a cabo)
Pillow==12.2.0           # ícone da clínica na etiqueta (PDF e bitmap ^GFA do ZPL)

# MOD-07 · Patrimônio — ColetaWebService (serviço HTTP headless de coleta)
flask==3.1.3             # rotas /p e /parear
waitress==3.0.2          # servidor WSGI puro-Python (AD-14)